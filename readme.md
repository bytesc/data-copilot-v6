




# graph

1. histogram – any bucket contains "from" or "to"
2. grouped_bar (separate sub-plots) – outer buckets have sub_aggregations.buckets with len>1 and meaningful key
3. bar+pie – single-layer buckets and any bucket’s metrics contains "percentage"
4. bar – single-layer buckets
